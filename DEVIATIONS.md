# DEVIATIONS – סטיות והבהרות ביחס למסמך האפיון

כל שורה: תאריך | סעיף באפיון | מה יושם | סיבה.

| תאריך | סעיף | מה יושם | סיבה |
|---|---|---|---|
| 13/09/2026 | 11.9 | ה-fixture "אדרת ברעננה" משתמש בתת-חוזה "תכנון מפורט" עם **בסיס 126,000 והנחה 10% → 113,400** (ולא "126,000 (בסיס 140,000)"). | סעיף 11.9 סותר את עצמו: 75,600 + 126,000 = 201,600, אך הסעיף (וגם תנאי הסיום בפרק 18) קובע סכום חוזה 189,000 ויתרה 181,440. הערכים ברמת החוזה (189,000 / 181,440) נלקחו כמחייבים; הם מתקיימים רק כאשר תת-חוזה "מפורט" = 113,400. |
| 13/09/2026 | 11.7 | `open_base` מחושב כ-`(opening_billed + Σ subtotal_base של חשבונות קודמים) − receipts_base`. | הנוסחה באפיון ("Σ subtotal_base קודמים − receipts_base") אינה כוללת יתרות פתיחה שחויבו לפני המערכת, ואז הזהות `subtotal_base = Σ amount_this` לא מתקיימת כשיש `opening_billed_pct`. התוספת שומרת על הזהות (נבדק ב-unit tests) ואינה משנה את הדוגמה בסעיף 11.9. הנחה #7 בפרק 19 ממילא דורשת אימות מול עידו. |
| 13/09/2026 | 2.1 | גרסאות: Next.js 15.5, React 19, Tailwind CSS 4 (עם `@tailwindcss/postcss`), zod 4, drizzle-orm 0.45. | הגרסאות העדכניות היציבות בזמן הפיתוח; Tailwind 4 תומך ב-logical properties ו-RTL באופן מובנה. |
| 13/09/2026 | 5.6 | טבלת `notifications` קיבלה עמודת `dedupe_key`; `users` קיבלה `invited_at`, `last_seen_at`; `departments` קיבלה `manager_user_id`; `documents` קיבלה `backed_up_at`; `invoice_lines` קיבלה `retainer_month`; `receipt_allocations` קיבלה שדות ביטול; נוספה טבלת lookup `document_types`. | נדרשים למימוש דרישות מפורשות (התראה פעם אחת לכל חצייה – 8.4; עותק למנהל מחלקה – 10.8; גיבוי לילי – 2.4; ביטול הקצאה עם סיבה – 11.10; "סוג מסמך (lookup עריך)" – 5.3). |
| 13/09/2026 | 5.3 | `sub_contracts.index_base_month` נוסף (null = יורש מהחוזה). | סעיף 11.7 קובע "תת-חוזה יורש, ניתן לדריסה" ולכן נדרש שדה. |
| 14/09/2026 | 11.6.1 | דיווחי שעות מסומנים ב-`invoice_id` כבר **ביצירת הטיוטה** (ולא רק באישור), ומשוחררים בביטול/הסרה מהטיוטה. | מונע כפל חיוב כששתי טיוטות נפתחות במקביל לאותו תת-חוזה. ההסרה של דיווח בודד מהטיוטה משחררת אותו לחשבון הבא כנדרש. |
| 14/09/2026 | 2.6 | לוחות הזמנים ב-`vercel.json` נקובים ב-UTC (Vercel Cron אינו תומך באזורי זמן): 09:00 ⇒ 06:00 UTC, 00:10 ⇒ 21:10 UTC (יום קודם) וכו'. | מגבלת הפלטפורמה; בחורף ההרצה מוקדמת בשעה. |
| 14/09/2026 | 2.4 #7 | `db-dump` דורש `pg_dump` בסביבת הריצה; ב-Vercel Serverless אינו זמין ולכן ה-job מדווח כשל (התראה לאדמין) והגיבוי הלוגי מסתמך על Supabase PITR עד להרצת ה-job מסביבה עם pg_dump (למשל GitHub Action). | מגבלת הפלטפורמה. |
| 14/09/2026 | 17 | Sentry ממומש כדיווח envelope ישיר ל-API של Sentry (`lib/observability/sentry.ts`) ללא ה-SDK. | ה-SDK הרשמי מוסיף wrapping לכל ה-build; המימוש הקל מכסה שגיאות Server Actions ו-cron ומספיק לשלב זה. ניתן להחליף ל-`@sentry/nextjs` ללא שינוי בקריאות. |
| 14/09/2026 | 15.2 #13 | ייבוא zip של מסמכים היסטוריים לפי מבנה `{work_number}/…` לא מומש כ-batch; קבצים מועלים דרך טאב "מסמכים" של הפרויקט/החוזה (ריבוי קבצים בגרירה). עמודת `pdf_file` בתבנית החשבונות שמורה לצירוף עתידי. | תלוי בכמות ובמבנה הקבצים שיתקבלו מהלקוח; יושלם בשלב ההגירה בפועל. |
| 14/09/2026 | 2.6 | בפריסה הראשונה `vercel.json` מכיל **2 משימות cron יומיות בלבד** (`cbs-index-fetch`, `retainer-invoices`) במקום 9. הרשימה המלאה שמורה ב-`vercel.pro.json`. | חשבון Vercel בתוכנית Hobby מוגבל ל-2 משימות cron בהרצה יומית, ופריסה עם יותר נדחית. שבע המשימות הנותרות ניתנות להרצה ידנית מול `/api/cron/<job>` עם `Authorization: Bearer $CRON_SECRET`. עם השדרוג ל-Pro מעתיקים את `vercel.pro.json` ל-`vercel.json`. |
| 14/09/2026 | 6.1 | **אין אכיפת אימות דו-שלבי.** ברירת המחדל של `security.mfa_required_roles` שונתה מ-`["admin"]` ל-`[]`. | החלטת הלקוח (14/09/2026), אחרי שהוצג שאימות דו-שלבי (TOTP) הוא תכונת Pro ב-Clerk ואינו זמין בחשבון הנוכחי. המנגנון עצמו נשאר במימוש: מסך `/mfa`, בדיקת `mfaRequired` ומסך ההגדרות. הפעלה מחדש היא הוספת תפקידים ב-`הגדרות ← אבטחה`, לאחר שדרוג Clerk והפעלת Authenticator application. |
| 16/09/2026 | 0 (עקרון 1) | כותרות ההתראות שנוצרות בצד השרת (`notifyEvent`/`notify` ב-`lib/invoices`, `lib/hours`, `lib/contracts`, `lib/jobs/*`) כתובות בעברית בקוד, ולא דרך מפתחות תרגום. | ההתראה נשמרת כטקסט ב-DB בזמן האירוע, לפני שידוע באיזו שפה יקרא אותה כל נמען. תרגום לפי שפת הנמען דורש שמירת מפתח+פרמטרים במקום טקסט ורינדור בזמן הצגה. כל המשתמשים כיום בעברית; נרשם להשלמה. |
